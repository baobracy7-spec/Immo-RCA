import {NextResponse} from "next/server"; import {createClient} from "@supabase/supabase-js";
export async function POST(req:Request){try{const b=await req.json();const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)return NextResponse.json({message:"Supabase n’est pas encore configuré sur Vercel."},{status:503});const supabase=createClient(url,key);const {data,error}=await supabase.auth.signUp({email:b.email,password:b.password,options:{data:{full_name:b.full_name,phone:b.phone}}});if(error)return NextResponse.json({message:error.message},{status:400});return NextResponse.json({ok:true,message:data.session?"Compte créé avec succès.":"Compte créé. Vérifiez votre e-mail pour confirmer votre adresse."});}catch{return NextResponse.json({message:"Erreur serveur."},{status:500})}}