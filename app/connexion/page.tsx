"use client"; import Link from "next/link"; import {useState} from "react";
export default function Connexion(){const [msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json();if(d.ok) location.href="/dashboard"; else setMsg(d.message||"Identifiants incorrects.")}catch{setMsg("Configuration Supabase nécessaire.")}}return <main className="auth"><div className="authbox"><Link className="logo" href="/">Immo RCA</Link><h1>Connexion</h1><form onSubmit={submit}><div className="field"><label>E-mail</label><input name="email" type="email" required/></div><div className="field"><label>Mot de passe</label><input name="password" type="password" required/></div><button className="btn primary full">Se connecter</button></form>{msg&&<p>{msg}</p>}<p><Link href="/mot-de-passe-oublie" style={{color:"#174ea6"}}>Mot de passe oublié ?</Link></p><p className="muted">Pas encore de compte ? <Link href="/inscription" style={{color:"#174ea6"}}>Créer un compte</Link></p></div></main>}