"use client";
import Link from "next/link"; import {useState} from "react";
export default function Inscription(){const [msg,setMsg]=useState("");const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setMsg("");const f=new FormData(e.currentTarget);if(f.get("password")!==f.get("confirm")){setMsg("Les mots de passe ne correspondent pas.");setLoading(false);return}try{const r=await fetch("/api/auth/signup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json();setMsg(d.message||"Compte créé. Vérifiez votre e-mail.");}catch{setMsg("Configuration Supabase nécessaire pour activer la création de compte.")}finally{setLoading(false)}}return <main className="auth"><div className="authbox"><Link className="logo" href="/">Immo RCA</Link><h1>Créer un compte</h1><p className="muted">Publiez vos biens et gérez vos favoris.</p><form onSubmit={submit}><div className="field"><label>Nom complet</label><input name="full_name" required/></div><div className="field"><label>Téléphone</label><input name="phone" type="tel" placeholder="+236" required/></div><div className="field"><label>E-mail</label><input name="email" type="email" required/></div><div className="field"><label>Mot de passe</label><input name="password" type="password" minLength={8} required/></div><div className="field"><label>Confirmer le mot de passe</label><input name="confirm" type="password" minLength={8} required/></div><label><input type="checkbox" required/> J’accepte les conditions d’utilisation.</label><button className="btn primary full" disabled={loading} style={{marginTop:18}}>{loading?"Création...":"Créer mon compte"}</button></form>{msg&&<p>{msg}</p>}<p className="muted">Déjà un compte ? <Link href="/connexion" style={{color:"#174ea6"}}>Se connecter</Link></p></div></main>}